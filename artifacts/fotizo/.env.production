# Production build config (public — VITE_ vars are embedded in the client bundle).
# Real auth against the hosted backend; data domains stay mocked until the
# backend implements them (products, orders, messages, ...).
VITE_USE_MOCKS=true
VITE_USE_MOCK_AUTH=false
VITE_API_BASE_URL=https://fotizo.onrender.com/api
