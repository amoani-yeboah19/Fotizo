# constants/

Shared constant values (enumerations, static option lists, config maps).

Candidates to extract here later: `CURRENCIES` (currently in `@/contexts/CurrencyContext`) and the
category list (currently in `@/services/mockData`). Not moved yet to avoid behavioral changes.
