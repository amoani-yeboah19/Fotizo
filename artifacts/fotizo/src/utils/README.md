# utils/

App-specific helper functions (formatting, guards, small pure utilities).

Note: the `cn()` class-name helper stays in `@/lib/utils` (referenced widely as `@/lib/utils`).
Put domain-neutral helpers here; keep feature-specific logic inside its feature folder.
