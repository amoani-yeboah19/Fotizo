# assets/

Bundled/imported assets (images, icons, fonts) that are referenced from code via `import`.

Note: existing images live in `/public` and are referenced by absolute URL (`/images/...`); they
stay there. Use this folder only for assets you import into modules and want Vite to fingerprint.
