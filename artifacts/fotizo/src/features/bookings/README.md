# features/bookings/

Service bookings domain (placeholder — no standalone pages yet).

Booking data currently lives in `@/services/mockData` (`mockBookings`) and is rendered inside the
buyer dashboard (`@/features/profile/dashboards/BuyerDashboard`). When a dedicated bookings
experience is built (list, detail, scheduling), its pages/components/hooks go here.
